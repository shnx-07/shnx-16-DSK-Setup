[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "shnx-backend"
version = "0.1.0"
description = "Optional Python backend for SHNX-SHELL"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["shnx_backend*"]
